address: :2222
apiVersion: v1
commands:
- handler: ''
  plugin: LLMHoneypot
  regex: ^(.+)$
cve_description: ''
cve_tags: []
deadlineTimeoutSeconds: 60
description: SSH interactive ChatGPT
passwordRegex: ^(root|qwerty|Smoker666|123456|jenkins|minecraft|sinus|alex|postgres|Ly123456|1234)$
plugin:
  llmModel: gpt-4o-mini
  llmProvider: openai
  openAISecretKey: placeholder
  prompt: You will act as an Ubuntu Linux terminal. The user will type commands, and
    you are to reply with what the terminal should show. Your responses must be contained
    within a single code block. Do not provide notes or explanations. Simulate realistic
    filesystem responses, process lists, or command output as needed.
protocol: ssh
serverName: ubuntu
